db, _ := sql.Open(SqlDriver, SqlSourceName)
defer db.Close()
println("尝试ping the 数据库")
if err := db.Ping(); err != nil {
	log.Fatal(err)
	return
}
fmt.Println("连接成功")
db.Query("update user_info set password="+new_password+" where phone="+phone)
